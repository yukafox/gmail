@import "variables.roo";

.grid_header        { height: $hH; }
.grid_left_aside    { position: absolute; left: 0; top: $hH; bottom: 0; width: $asideW; }
.grid_main_content  { position: absolute; left: $asideW; top: $hH; right: 0; bottom: 0; overflow:auto; }
